<% if (locals.errors) { %>
    <dialog id="err-dialog">
        <% errors.forEach(err => { %>
            <p><%= err.msg %></p>
        <%})%>
        <div><button commandfor="err-dialog" command="close">Close</button></div>
    </dialog>

    <script src="/scripts/errorDialogInteractions.js" defer></script>
<%}%>
